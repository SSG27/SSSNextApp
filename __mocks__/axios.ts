// // __mocks__/axios.ts

// const axiosMock: any = jest.createMockFromModule('axios');

// // Mocking the `request` function to return a resolved promise with mocked data
// axiosMock.request = jest.fn((options: any) => {
//   return Promise.resolve({ data: { result: [{ type: 'mock', title: 'Mock Title', streamingInfo: { gb: [] } }] } });
// });

// export default axiosMock;
